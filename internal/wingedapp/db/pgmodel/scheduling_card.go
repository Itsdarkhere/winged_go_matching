// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package pgmodel

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// SchedulingCard is an object representing the database table.
type SchedulingCard struct {
	ID                string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	DateInstanceRefID string    `boil:"date_instance_ref_id" json:"date_instance_ref_id" toml:"date_instance_ref_id" yaml:"date_instance_ref_id"`
	UserRefID         string    `boil:"user_ref_id" json:"user_ref_id" toml:"user_ref_id" yaml:"user_ref_id"`
	CardType          string    `boil:"card_type" json:"card_type" toml:"card_type" yaml:"card_type"`
	CardState         string    `boil:"card_state" json:"card_state" toml:"card_state" yaml:"card_state"`
	Payload           null.JSON `boil:"payload" json:"payload,omitempty" toml:"payload" yaml:"payload,omitempty"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	CompletedAt       null.Time `boil:"completed_at" json:"completed_at,omitempty" toml:"completed_at" yaml:"completed_at,omitempty"`
	ExpiredAt         null.Time `boil:"expired_at" json:"expired_at,omitempty" toml:"expired_at" yaml:"expired_at,omitempty"`

	R *schedulingCardR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L schedulingCardL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SchedulingCardColumns = struct {
	ID                string
	DateInstanceRefID string
	UserRefID         string
	CardType          string
	CardState         string
	Payload           string
	CreatedAt         string
	CompletedAt       string
	ExpiredAt         string
}{
	ID:                "id",
	DateInstanceRefID: "date_instance_ref_id",
	UserRefID:         "user_ref_id",
	CardType:          "card_type",
	CardState:         "card_state",
	Payload:           "payload",
	CreatedAt:         "created_at",
	CompletedAt:       "completed_at",
	ExpiredAt:         "expired_at",
}

var SchedulingCardTableColumns = struct {
	ID                string
	DateInstanceRefID string
	UserRefID         string
	CardType          string
	CardState         string
	Payload           string
	CreatedAt         string
	CompletedAt       string
	ExpiredAt         string
}{
	ID:                "scheduling_card.id",
	DateInstanceRefID: "scheduling_card.date_instance_ref_id",
	UserRefID:         "scheduling_card.user_ref_id",
	CardType:          "scheduling_card.card_type",
	CardState:         "scheduling_card.card_state",
	Payload:           "scheduling_card.payload",
	CreatedAt:         "scheduling_card.created_at",
	CompletedAt:       "scheduling_card.completed_at",
	ExpiredAt:         "scheduling_card.expired_at",
}

// Generated where

var SchedulingCardWhere = struct {
	ID                whereHelperstring
	DateInstanceRefID whereHelperstring
	UserRefID         whereHelperstring
	CardType          whereHelperstring
	CardState         whereHelperstring
	Payload           whereHelpernull_JSON
	CreatedAt         whereHelpertime_Time
	CompletedAt       whereHelpernull_Time
	ExpiredAt         whereHelpernull_Time
}{
	ID:                whereHelperstring{field: "\"scheduling_card\".\"id\""},
	DateInstanceRefID: whereHelperstring{field: "\"scheduling_card\".\"date_instance_ref_id\""},
	UserRefID:         whereHelperstring{field: "\"scheduling_card\".\"user_ref_id\""},
	CardType:          whereHelperstring{field: "\"scheduling_card\".\"card_type\""},
	CardState:         whereHelperstring{field: "\"scheduling_card\".\"card_state\""},
	Payload:           whereHelpernull_JSON{field: "\"scheduling_card\".\"payload\""},
	CreatedAt:         whereHelpertime_Time{field: "\"scheduling_card\".\"created_at\""},
	CompletedAt:       whereHelpernull_Time{field: "\"scheduling_card\".\"completed_at\""},
	ExpiredAt:         whereHelpernull_Time{field: "\"scheduling_card\".\"expired_at\""},
}

// SchedulingCardRels is where relationship names are stored.
var SchedulingCardRels = struct {
	DateInstanceRef string
	UserRef         string
}{
	DateInstanceRef: "DateInstanceRef",
	UserRef:         "UserRef",
}

// schedulingCardR is where relationships are stored.
type schedulingCardR struct {
	DateInstanceRef *DateInstance `boil:"DateInstanceRef" json:"DateInstanceRef" toml:"DateInstanceRef" yaml:"DateInstanceRef"`
	UserRef         *User         `boil:"UserRef" json:"UserRef" toml:"UserRef" yaml:"UserRef"`
}

// NewStruct creates a new relationship struct
func (*schedulingCardR) NewStruct() *schedulingCardR {
	return &schedulingCardR{}
}

func (o *SchedulingCard) GetDateInstanceRef() *DateInstance {
	if o == nil {
		return nil
	}

	return o.R.GetDateInstanceRef()
}

func (r *schedulingCardR) GetDateInstanceRef() *DateInstance {
	if r == nil {
		return nil
	}

	return r.DateInstanceRef
}

func (o *SchedulingCard) GetUserRef() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUserRef()
}

func (r *schedulingCardR) GetUserRef() *User {
	if r == nil {
		return nil
	}

	return r.UserRef
}

// schedulingCardL is where Load methods for each relationship are stored.
type schedulingCardL struct{}

var (
	schedulingCardAllColumns            = []string{"id", "date_instance_ref_id", "user_ref_id", "card_type", "card_state", "payload", "created_at", "completed_at", "expired_at"}
	schedulingCardColumnsWithoutDefault = []string{"date_instance_ref_id", "user_ref_id", "card_type"}
	schedulingCardColumnsWithDefault    = []string{"id", "card_state", "payload", "created_at", "completed_at", "expired_at"}
	schedulingCardPrimaryKeyColumns     = []string{"id"}
	schedulingCardGeneratedColumns      = []string{}
)

type (
	// SchedulingCardSlice is an alias for a slice of pointers to SchedulingCard.
	// This should almost always be used instead of []SchedulingCard.
	SchedulingCardSlice []*SchedulingCard
	// SchedulingCardHook is the signature for custom SchedulingCard hook methods
	SchedulingCardHook func(context.Context, boil.ContextExecutor, *SchedulingCard) error

	schedulingCardQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	schedulingCardType                 = reflect.TypeOf(&SchedulingCard{})
	schedulingCardMapping              = queries.MakeStructMapping(schedulingCardType)
	schedulingCardPrimaryKeyMapping, _ = queries.BindMapping(schedulingCardType, schedulingCardMapping, schedulingCardPrimaryKeyColumns)
	schedulingCardInsertCacheMut       sync.RWMutex
	schedulingCardInsertCache          = make(map[string]insertCache)
	schedulingCardUpdateCacheMut       sync.RWMutex
	schedulingCardUpdateCache          = make(map[string]updateCache)
	schedulingCardUpsertCacheMut       sync.RWMutex
	schedulingCardUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var schedulingCardAfterSelectMu sync.Mutex
var schedulingCardAfterSelectHooks []SchedulingCardHook

var schedulingCardBeforeInsertMu sync.Mutex
var schedulingCardBeforeInsertHooks []SchedulingCardHook
var schedulingCardAfterInsertMu sync.Mutex
var schedulingCardAfterInsertHooks []SchedulingCardHook

var schedulingCardBeforeUpdateMu sync.Mutex
var schedulingCardBeforeUpdateHooks []SchedulingCardHook
var schedulingCardAfterUpdateMu sync.Mutex
var schedulingCardAfterUpdateHooks []SchedulingCardHook

var schedulingCardBeforeDeleteMu sync.Mutex
var schedulingCardBeforeDeleteHooks []SchedulingCardHook
var schedulingCardAfterDeleteMu sync.Mutex
var schedulingCardAfterDeleteHooks []SchedulingCardHook

var schedulingCardBeforeUpsertMu sync.Mutex
var schedulingCardBeforeUpsertHooks []SchedulingCardHook
var schedulingCardAfterUpsertMu sync.Mutex
var schedulingCardAfterUpsertHooks []SchedulingCardHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SchedulingCard) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SchedulingCard) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SchedulingCard) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SchedulingCard) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SchedulingCard) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SchedulingCard) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SchedulingCard) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SchedulingCard) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SchedulingCard) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range schedulingCardAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSchedulingCardHook registers your hook function for all future operations.
func AddSchedulingCardHook(hookPoint boil.HookPoint, schedulingCardHook SchedulingCardHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		schedulingCardAfterSelectMu.Lock()
		schedulingCardAfterSelectHooks = append(schedulingCardAfterSelectHooks, schedulingCardHook)
		schedulingCardAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		schedulingCardBeforeInsertMu.Lock()
		schedulingCardBeforeInsertHooks = append(schedulingCardBeforeInsertHooks, schedulingCardHook)
		schedulingCardBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		schedulingCardAfterInsertMu.Lock()
		schedulingCardAfterInsertHooks = append(schedulingCardAfterInsertHooks, schedulingCardHook)
		schedulingCardAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		schedulingCardBeforeUpdateMu.Lock()
		schedulingCardBeforeUpdateHooks = append(schedulingCardBeforeUpdateHooks, schedulingCardHook)
		schedulingCardBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		schedulingCardAfterUpdateMu.Lock()
		schedulingCardAfterUpdateHooks = append(schedulingCardAfterUpdateHooks, schedulingCardHook)
		schedulingCardAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		schedulingCardBeforeDeleteMu.Lock()
		schedulingCardBeforeDeleteHooks = append(schedulingCardBeforeDeleteHooks, schedulingCardHook)
		schedulingCardBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		schedulingCardAfterDeleteMu.Lock()
		schedulingCardAfterDeleteHooks = append(schedulingCardAfterDeleteHooks, schedulingCardHook)
		schedulingCardAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		schedulingCardBeforeUpsertMu.Lock()
		schedulingCardBeforeUpsertHooks = append(schedulingCardBeforeUpsertHooks, schedulingCardHook)
		schedulingCardBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		schedulingCardAfterUpsertMu.Lock()
		schedulingCardAfterUpsertHooks = append(schedulingCardAfterUpsertHooks, schedulingCardHook)
		schedulingCardAfterUpsertMu.Unlock()
	}
}

// One returns a single schedulingCard record from the query.
func (q schedulingCardQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SchedulingCard, error) {
	o := &SchedulingCard{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "pgmodel: failed to execute a one query for scheduling_card")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all SchedulingCard records from the query.
func (q schedulingCardQuery) All(ctx context.Context, exec boil.ContextExecutor) (SchedulingCardSlice, error) {
	var o []*SchedulingCard

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "pgmodel: failed to assign all query results to SchedulingCard slice")
	}

	if len(schedulingCardAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all SchedulingCard records in the query.
func (q schedulingCardQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to count scheduling_card rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q schedulingCardQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "pgmodel: failed to check if scheduling_card exists")
	}

	return count > 0, nil
}

// DateInstanceRef pointed to by the foreign key.
func (o *SchedulingCard) DateInstanceRef(mods ...qm.QueryMod) dateInstanceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.DateInstanceRefID),
	}

	queryMods = append(queryMods, mods...)

	return DateInstances(queryMods...)
}

// UserRef pointed to by the foreign key.
func (o *SchedulingCard) UserRef(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserRefID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadDateInstanceRef allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (schedulingCardL) LoadDateInstanceRef(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSchedulingCard interface{}, mods queries.Applicator) error {
	var slice []*SchedulingCard
	var object *SchedulingCard

	if singular {
		var ok bool
		object, ok = maybeSchedulingCard.(*SchedulingCard)
		if !ok {
			object = new(SchedulingCard)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSchedulingCard)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSchedulingCard))
			}
		}
	} else {
		s, ok := maybeSchedulingCard.(*[]*SchedulingCard)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSchedulingCard)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSchedulingCard))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &schedulingCardR{}
		}
		args[object.DateInstanceRefID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &schedulingCardR{}
			}

			args[obj.DateInstanceRefID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`date_instance`),
		qm.WhereIn(`date_instance.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DateInstance")
	}

	var resultSlice []*DateInstance
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DateInstance")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for date_instance")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for date_instance")
	}

	if len(dateInstanceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DateInstanceRef = foreign
		if foreign.R == nil {
			foreign.R = &dateInstanceR{}
		}
		foreign.R.DateInstanceRefSchedulingCards = append(foreign.R.DateInstanceRefSchedulingCards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DateInstanceRefID == foreign.ID {
				local.R.DateInstanceRef = foreign
				if foreign.R == nil {
					foreign.R = &dateInstanceR{}
				}
				foreign.R.DateInstanceRefSchedulingCards = append(foreign.R.DateInstanceRefSchedulingCards, local)
				break
			}
		}
	}

	return nil
}

// LoadUserRef allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (schedulingCardL) LoadUserRef(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSchedulingCard interface{}, mods queries.Applicator) error {
	var slice []*SchedulingCard
	var object *SchedulingCard

	if singular {
		var ok bool
		object, ok = maybeSchedulingCard.(*SchedulingCard)
		if !ok {
			object = new(SchedulingCard)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSchedulingCard)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSchedulingCard))
			}
		}
	} else {
		s, ok := maybeSchedulingCard.(*[]*SchedulingCard)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSchedulingCard)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSchedulingCard))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &schedulingCardR{}
		}
		args[object.UserRefID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &schedulingCardR{}
			}

			args[obj.UserRefID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.UserRef = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserRefSchedulingCards = append(foreign.R.UserRefSchedulingCards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserRefID == foreign.ID {
				local.R.UserRef = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserRefSchedulingCards = append(foreign.R.UserRefSchedulingCards, local)
				break
			}
		}
	}

	return nil
}

// SetDateInstanceRef of the schedulingCard to the related item.
// Sets o.R.DateInstanceRef to related.
// Adds o to related.R.DateInstanceRefSchedulingCards.
func (o *SchedulingCard) SetDateInstanceRef(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DateInstance) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"scheduling_card\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"date_instance_ref_id"}),
		strmangle.WhereClause("\"", "\"", 2, schedulingCardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DateInstanceRefID = related.ID
	if o.R == nil {
		o.R = &schedulingCardR{
			DateInstanceRef: related,
		}
	} else {
		o.R.DateInstanceRef = related
	}

	if related.R == nil {
		related.R = &dateInstanceR{
			DateInstanceRefSchedulingCards: SchedulingCardSlice{o},
		}
	} else {
		related.R.DateInstanceRefSchedulingCards = append(related.R.DateInstanceRefSchedulingCards, o)
	}

	return nil
}

// SetUserRef of the schedulingCard to the related item.
// Sets o.R.UserRef to related.
// Adds o to related.R.UserRefSchedulingCards.
func (o *SchedulingCard) SetUserRef(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"scheduling_card\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_ref_id"}),
		strmangle.WhereClause("\"", "\"", 2, schedulingCardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserRefID = related.ID
	if o.R == nil {
		o.R = &schedulingCardR{
			UserRef: related,
		}
	} else {
		o.R.UserRef = related
	}

	if related.R == nil {
		related.R = &userR{
			UserRefSchedulingCards: SchedulingCardSlice{o},
		}
	} else {
		related.R.UserRefSchedulingCards = append(related.R.UserRefSchedulingCards, o)
	}

	return nil
}

// SchedulingCards retrieves all the records using an executor.
func SchedulingCards(mods ...qm.QueryMod) schedulingCardQuery {
	mods = append(mods, qm.From("\"scheduling_card\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"scheduling_card\".*"})
	}

	return schedulingCardQuery{q}
}

// FindSchedulingCard retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSchedulingCard(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SchedulingCard, error) {
	schedulingCardObj := &SchedulingCard{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"scheduling_card\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, schedulingCardObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "pgmodel: unable to select from scheduling_card")
	}

	if err = schedulingCardObj.doAfterSelectHooks(ctx, exec); err != nil {
		return schedulingCardObj, err
	}

	return schedulingCardObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SchedulingCard) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("pgmodel: no scheduling_card provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(schedulingCardColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	schedulingCardInsertCacheMut.RLock()
	cache, cached := schedulingCardInsertCache[key]
	schedulingCardInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			schedulingCardAllColumns,
			schedulingCardColumnsWithDefault,
			schedulingCardColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(schedulingCardType, schedulingCardMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(schedulingCardType, schedulingCardMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"scheduling_card\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"scheduling_card\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to insert into scheduling_card")
	}

	if !cached {
		schedulingCardInsertCacheMut.Lock()
		schedulingCardInsertCache[key] = cache
		schedulingCardInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the SchedulingCard.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SchedulingCard) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	schedulingCardUpdateCacheMut.RLock()
	cache, cached := schedulingCardUpdateCache[key]
	schedulingCardUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			schedulingCardAllColumns,
			schedulingCardPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("pgmodel: unable to update scheduling_card, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"scheduling_card\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, schedulingCardPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(schedulingCardType, schedulingCardMapping, append(wl, schedulingCardPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update scheduling_card row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by update for scheduling_card")
	}

	if !cached {
		schedulingCardUpdateCacheMut.Lock()
		schedulingCardUpdateCache[key] = cache
		schedulingCardUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q schedulingCardQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update all for scheduling_card")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to retrieve rows affected for scheduling_card")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SchedulingCardSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("pgmodel: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), schedulingCardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"scheduling_card\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, schedulingCardPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update all in schedulingCard slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to retrieve rows affected all in update all schedulingCard")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SchedulingCard) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("pgmodel: no scheduling_card provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(schedulingCardColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	schedulingCardUpsertCacheMut.RLock()
	cache, cached := schedulingCardUpsertCache[key]
	schedulingCardUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			schedulingCardAllColumns,
			schedulingCardColumnsWithDefault,
			schedulingCardColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			schedulingCardAllColumns,
			schedulingCardPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("pgmodel: unable to upsert scheduling_card, could not build update column list")
		}

		ret := strmangle.SetComplement(schedulingCardAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(schedulingCardPrimaryKeyColumns) == 0 {
				return errors.New("pgmodel: unable to upsert scheduling_card, could not build conflict column list")
			}

			conflict = make([]string, len(schedulingCardPrimaryKeyColumns))
			copy(conflict, schedulingCardPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"scheduling_card\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(schedulingCardType, schedulingCardMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(schedulingCardType, schedulingCardMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to upsert scheduling_card")
	}

	if !cached {
		schedulingCardUpsertCacheMut.Lock()
		schedulingCardUpsertCache[key] = cache
		schedulingCardUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single SchedulingCard record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SchedulingCard) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("pgmodel: no SchedulingCard provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), schedulingCardPrimaryKeyMapping)
	sql := "DELETE FROM \"scheduling_card\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete from scheduling_card")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by delete for scheduling_card")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q schedulingCardQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("pgmodel: no schedulingCardQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete all from scheduling_card")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by deleteall for scheduling_card")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SchedulingCardSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(schedulingCardBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), schedulingCardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"scheduling_card\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, schedulingCardPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete all from schedulingCard slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by deleteall for scheduling_card")
	}

	if len(schedulingCardAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SchedulingCard) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSchedulingCard(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SchedulingCardSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SchedulingCardSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), schedulingCardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"scheduling_card\".* FROM \"scheduling_card\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, schedulingCardPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to reload all in SchedulingCardSlice")
	}

	*o = slice

	return nil
}

// SchedulingCardExists checks if the SchedulingCard row exists.
func SchedulingCardExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"scheduling_card\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "pgmodel: unable to check if scheduling_card exists")
	}

	return exists, nil
}

// Exists checks if the SchedulingCard row exists.
func (o *SchedulingCard) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SchedulingCardExists(ctx, exec, o.ID)
}
