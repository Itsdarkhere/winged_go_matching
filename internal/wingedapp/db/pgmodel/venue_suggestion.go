// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package pgmodel

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// VenueSuggestion is an object representing the database table.
type VenueSuggestion struct {
	ID                string `boil:"id" json:"id" toml:"id" yaml:"id"`
	DateInstanceRefID string `boil:"date_instance_ref_id" json:"date_instance_ref_id" toml:"date_instance_ref_id" yaml:"date_instance_ref_id"`
	SuggestedByRefID  string `boil:"suggested_by_ref_id" json:"suggested_by_ref_id" toml:"suggested_by_ref_id" yaml:"suggested_by_ref_id"`
	// Google Maps link or similar URL provided by receiver
	VenueLink null.String `boil:"venue_link" json:"venue_link,omitempty" toml:"venue_link" yaml:"venue_link,omitempty"`
	// Venue name typed by receiver (used with venue_area for resolution)
	VenueName          null.String `boil:"venue_name" json:"venue_name,omitempty" toml:"venue_name" yaml:"venue_name,omitempty"`
	VenueArea          null.String `boil:"venue_area" json:"venue_area,omitempty" toml:"venue_area" yaml:"venue_area,omitempty"`
	ResolvedVenueRefID null.String `boil:"resolved_venue_ref_id" json:"resolved_venue_ref_id,omitempty" toml:"resolved_venue_ref_id" yaml:"resolved_venue_ref_id,omitempty"`
	// pending=not yet processed, resolved=venue found, failed=could not resolve
	ResolutionStatus string      `boil:"resolution_status" json:"resolution_status" toml:"resolution_status" yaml:"resolution_status"`
	ResolutionError  null.String `boil:"resolution_error" json:"resolution_error,omitempty" toml:"resolution_error" yaml:"resolution_error,omitempty"`
	// accepted=initiator accepted suggestion, show_alternatives=initiator wants similar venues
	InitiatorResponse null.String `boil:"initiator_response" json:"initiator_response,omitempty" toml:"initiator_response" yaml:"initiator_response,omitempty"`
	RespondedAt       null.Time   `boil:"responded_at" json:"responded_at,omitempty" toml:"responded_at" yaml:"responded_at,omitempty"`
	CreatedAt         time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *venueSuggestionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L venueSuggestionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var VenueSuggestionColumns = struct {
	ID                 string
	DateInstanceRefID  string
	SuggestedByRefID   string
	VenueLink          string
	VenueName          string
	VenueArea          string
	ResolvedVenueRefID string
	ResolutionStatus   string
	ResolutionError    string
	InitiatorResponse  string
	RespondedAt        string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "id",
	DateInstanceRefID:  "date_instance_ref_id",
	SuggestedByRefID:   "suggested_by_ref_id",
	VenueLink:          "venue_link",
	VenueName:          "venue_name",
	VenueArea:          "venue_area",
	ResolvedVenueRefID: "resolved_venue_ref_id",
	ResolutionStatus:   "resolution_status",
	ResolutionError:    "resolution_error",
	InitiatorResponse:  "initiator_response",
	RespondedAt:        "responded_at",
	CreatedAt:          "created_at",
	UpdatedAt:          "updated_at",
}

var VenueSuggestionTableColumns = struct {
	ID                 string
	DateInstanceRefID  string
	SuggestedByRefID   string
	VenueLink          string
	VenueName          string
	VenueArea          string
	ResolvedVenueRefID string
	ResolutionStatus   string
	ResolutionError    string
	InitiatorResponse  string
	RespondedAt        string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "venue_suggestion.id",
	DateInstanceRefID:  "venue_suggestion.date_instance_ref_id",
	SuggestedByRefID:   "venue_suggestion.suggested_by_ref_id",
	VenueLink:          "venue_suggestion.venue_link",
	VenueName:          "venue_suggestion.venue_name",
	VenueArea:          "venue_suggestion.venue_area",
	ResolvedVenueRefID: "venue_suggestion.resolved_venue_ref_id",
	ResolutionStatus:   "venue_suggestion.resolution_status",
	ResolutionError:    "venue_suggestion.resolution_error",
	InitiatorResponse:  "venue_suggestion.initiator_response",
	RespondedAt:        "venue_suggestion.responded_at",
	CreatedAt:          "venue_suggestion.created_at",
	UpdatedAt:          "venue_suggestion.updated_at",
}

// Generated where

var VenueSuggestionWhere = struct {
	ID                 whereHelperstring
	DateInstanceRefID  whereHelperstring
	SuggestedByRefID   whereHelperstring
	VenueLink          whereHelpernull_String
	VenueName          whereHelpernull_String
	VenueArea          whereHelpernull_String
	ResolvedVenueRefID whereHelpernull_String
	ResolutionStatus   whereHelperstring
	ResolutionError    whereHelpernull_String
	InitiatorResponse  whereHelpernull_String
	RespondedAt        whereHelpernull_Time
	CreatedAt          whereHelpertime_Time
	UpdatedAt          whereHelpernull_Time
}{
	ID:                 whereHelperstring{field: "\"venue_suggestion\".\"id\""},
	DateInstanceRefID:  whereHelperstring{field: "\"venue_suggestion\".\"date_instance_ref_id\""},
	SuggestedByRefID:   whereHelperstring{field: "\"venue_suggestion\".\"suggested_by_ref_id\""},
	VenueLink:          whereHelpernull_String{field: "\"venue_suggestion\".\"venue_link\""},
	VenueName:          whereHelpernull_String{field: "\"venue_suggestion\".\"venue_name\""},
	VenueArea:          whereHelpernull_String{field: "\"venue_suggestion\".\"venue_area\""},
	ResolvedVenueRefID: whereHelpernull_String{field: "\"venue_suggestion\".\"resolved_venue_ref_id\""},
	ResolutionStatus:   whereHelperstring{field: "\"venue_suggestion\".\"resolution_status\""},
	ResolutionError:    whereHelpernull_String{field: "\"venue_suggestion\".\"resolution_error\""},
	InitiatorResponse:  whereHelpernull_String{field: "\"venue_suggestion\".\"initiator_response\""},
	RespondedAt:        whereHelpernull_Time{field: "\"venue_suggestion\".\"responded_at\""},
	CreatedAt:          whereHelpertime_Time{field: "\"venue_suggestion\".\"created_at\""},
	UpdatedAt:          whereHelpernull_Time{field: "\"venue_suggestion\".\"updated_at\""},
}

// VenueSuggestionRels is where relationship names are stored.
var VenueSuggestionRels = struct {
	DateInstanceRef  string
	ResolvedVenueRef string
	SuggestedByRef   string
}{
	DateInstanceRef:  "DateInstanceRef",
	ResolvedVenueRef: "ResolvedVenueRef",
	SuggestedByRef:   "SuggestedByRef",
}

// venueSuggestionR is where relationships are stored.
type venueSuggestionR struct {
	DateInstanceRef  *DateInstance `boil:"DateInstanceRef" json:"DateInstanceRef" toml:"DateInstanceRef" yaml:"DateInstanceRef"`
	ResolvedVenueRef *Venue        `boil:"ResolvedVenueRef" json:"ResolvedVenueRef" toml:"ResolvedVenueRef" yaml:"ResolvedVenueRef"`
	SuggestedByRef   *User         `boil:"SuggestedByRef" json:"SuggestedByRef" toml:"SuggestedByRef" yaml:"SuggestedByRef"`
}

// NewStruct creates a new relationship struct
func (*venueSuggestionR) NewStruct() *venueSuggestionR {
	return &venueSuggestionR{}
}

func (o *VenueSuggestion) GetDateInstanceRef() *DateInstance {
	if o == nil {
		return nil
	}

	return o.R.GetDateInstanceRef()
}

func (r *venueSuggestionR) GetDateInstanceRef() *DateInstance {
	if r == nil {
		return nil
	}

	return r.DateInstanceRef
}

func (o *VenueSuggestion) GetResolvedVenueRef() *Venue {
	if o == nil {
		return nil
	}

	return o.R.GetResolvedVenueRef()
}

func (r *venueSuggestionR) GetResolvedVenueRef() *Venue {
	if r == nil {
		return nil
	}

	return r.ResolvedVenueRef
}

func (o *VenueSuggestion) GetSuggestedByRef() *User {
	if o == nil {
		return nil
	}

	return o.R.GetSuggestedByRef()
}

func (r *venueSuggestionR) GetSuggestedByRef() *User {
	if r == nil {
		return nil
	}

	return r.SuggestedByRef
}

// venueSuggestionL is where Load methods for each relationship are stored.
type venueSuggestionL struct{}

var (
	venueSuggestionAllColumns            = []string{"id", "date_instance_ref_id", "suggested_by_ref_id", "venue_link", "venue_name", "venue_area", "resolved_venue_ref_id", "resolution_status", "resolution_error", "initiator_response", "responded_at", "created_at", "updated_at"}
	venueSuggestionColumnsWithoutDefault = []string{"date_instance_ref_id", "suggested_by_ref_id"}
	venueSuggestionColumnsWithDefault    = []string{"id", "venue_link", "venue_name", "venue_area", "resolved_venue_ref_id", "resolution_status", "resolution_error", "initiator_response", "responded_at", "created_at", "updated_at"}
	venueSuggestionPrimaryKeyColumns     = []string{"id"}
	venueSuggestionGeneratedColumns      = []string{}
)

type (
	// VenueSuggestionSlice is an alias for a slice of pointers to VenueSuggestion.
	// This should almost always be used instead of []VenueSuggestion.
	VenueSuggestionSlice []*VenueSuggestion
	// VenueSuggestionHook is the signature for custom VenueSuggestion hook methods
	VenueSuggestionHook func(context.Context, boil.ContextExecutor, *VenueSuggestion) error

	venueSuggestionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	venueSuggestionType                 = reflect.TypeOf(&VenueSuggestion{})
	venueSuggestionMapping              = queries.MakeStructMapping(venueSuggestionType)
	venueSuggestionPrimaryKeyMapping, _ = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, venueSuggestionPrimaryKeyColumns)
	venueSuggestionInsertCacheMut       sync.RWMutex
	venueSuggestionInsertCache          = make(map[string]insertCache)
	venueSuggestionUpdateCacheMut       sync.RWMutex
	venueSuggestionUpdateCache          = make(map[string]updateCache)
	venueSuggestionUpsertCacheMut       sync.RWMutex
	venueSuggestionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var venueSuggestionAfterSelectMu sync.Mutex
var venueSuggestionAfterSelectHooks []VenueSuggestionHook

var venueSuggestionBeforeInsertMu sync.Mutex
var venueSuggestionBeforeInsertHooks []VenueSuggestionHook
var venueSuggestionAfterInsertMu sync.Mutex
var venueSuggestionAfterInsertHooks []VenueSuggestionHook

var venueSuggestionBeforeUpdateMu sync.Mutex
var venueSuggestionBeforeUpdateHooks []VenueSuggestionHook
var venueSuggestionAfterUpdateMu sync.Mutex
var venueSuggestionAfterUpdateHooks []VenueSuggestionHook

var venueSuggestionBeforeDeleteMu sync.Mutex
var venueSuggestionBeforeDeleteHooks []VenueSuggestionHook
var venueSuggestionAfterDeleteMu sync.Mutex
var venueSuggestionAfterDeleteHooks []VenueSuggestionHook

var venueSuggestionBeforeUpsertMu sync.Mutex
var venueSuggestionBeforeUpsertHooks []VenueSuggestionHook
var venueSuggestionAfterUpsertMu sync.Mutex
var venueSuggestionAfterUpsertHooks []VenueSuggestionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *VenueSuggestion) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *VenueSuggestion) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *VenueSuggestion) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *VenueSuggestion) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *VenueSuggestion) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *VenueSuggestion) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *VenueSuggestion) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *VenueSuggestion) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *VenueSuggestion) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range venueSuggestionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddVenueSuggestionHook registers your hook function for all future operations.
func AddVenueSuggestionHook(hookPoint boil.HookPoint, venueSuggestionHook VenueSuggestionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		venueSuggestionAfterSelectMu.Lock()
		venueSuggestionAfterSelectHooks = append(venueSuggestionAfterSelectHooks, venueSuggestionHook)
		venueSuggestionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		venueSuggestionBeforeInsertMu.Lock()
		venueSuggestionBeforeInsertHooks = append(venueSuggestionBeforeInsertHooks, venueSuggestionHook)
		venueSuggestionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		venueSuggestionAfterInsertMu.Lock()
		venueSuggestionAfterInsertHooks = append(venueSuggestionAfterInsertHooks, venueSuggestionHook)
		venueSuggestionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		venueSuggestionBeforeUpdateMu.Lock()
		venueSuggestionBeforeUpdateHooks = append(venueSuggestionBeforeUpdateHooks, venueSuggestionHook)
		venueSuggestionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		venueSuggestionAfterUpdateMu.Lock()
		venueSuggestionAfterUpdateHooks = append(venueSuggestionAfterUpdateHooks, venueSuggestionHook)
		venueSuggestionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		venueSuggestionBeforeDeleteMu.Lock()
		venueSuggestionBeforeDeleteHooks = append(venueSuggestionBeforeDeleteHooks, venueSuggestionHook)
		venueSuggestionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		venueSuggestionAfterDeleteMu.Lock()
		venueSuggestionAfterDeleteHooks = append(venueSuggestionAfterDeleteHooks, venueSuggestionHook)
		venueSuggestionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		venueSuggestionBeforeUpsertMu.Lock()
		venueSuggestionBeforeUpsertHooks = append(venueSuggestionBeforeUpsertHooks, venueSuggestionHook)
		venueSuggestionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		venueSuggestionAfterUpsertMu.Lock()
		venueSuggestionAfterUpsertHooks = append(venueSuggestionAfterUpsertHooks, venueSuggestionHook)
		venueSuggestionAfterUpsertMu.Unlock()
	}
}

// One returns a single venueSuggestion record from the query.
func (q venueSuggestionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*VenueSuggestion, error) {
	o := &VenueSuggestion{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "pgmodel: failed to execute a one query for venue_suggestion")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all VenueSuggestion records from the query.
func (q venueSuggestionQuery) All(ctx context.Context, exec boil.ContextExecutor) (VenueSuggestionSlice, error) {
	var o []*VenueSuggestion

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "pgmodel: failed to assign all query results to VenueSuggestion slice")
	}

	if len(venueSuggestionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all VenueSuggestion records in the query.
func (q venueSuggestionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to count venue_suggestion rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q venueSuggestionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "pgmodel: failed to check if venue_suggestion exists")
	}

	return count > 0, nil
}

// DateInstanceRef pointed to by the foreign key.
func (o *VenueSuggestion) DateInstanceRef(mods ...qm.QueryMod) dateInstanceQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.DateInstanceRefID),
	}

	queryMods = append(queryMods, mods...)

	return DateInstances(queryMods...)
}

// ResolvedVenueRef pointed to by the foreign key.
func (o *VenueSuggestion) ResolvedVenueRef(mods ...qm.QueryMod) venueQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ResolvedVenueRefID),
	}

	queryMods = append(queryMods, mods...)

	return Venues(queryMods...)
}

// SuggestedByRef pointed to by the foreign key.
func (o *VenueSuggestion) SuggestedByRef(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SuggestedByRefID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadDateInstanceRef allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (venueSuggestionL) LoadDateInstanceRef(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVenueSuggestion interface{}, mods queries.Applicator) error {
	var slice []*VenueSuggestion
	var object *VenueSuggestion

	if singular {
		var ok bool
		object, ok = maybeVenueSuggestion.(*VenueSuggestion)
		if !ok {
			object = new(VenueSuggestion)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVenueSuggestion))
			}
		}
	} else {
		s, ok := maybeVenueSuggestion.(*[]*VenueSuggestion)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVenueSuggestion))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &venueSuggestionR{}
		}
		args[object.DateInstanceRefID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &venueSuggestionR{}
			}

			args[obj.DateInstanceRefID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`date_instance`),
		qm.WhereIn(`date_instance.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DateInstance")
	}

	var resultSlice []*DateInstance
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DateInstance")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for date_instance")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for date_instance")
	}

	if len(dateInstanceAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DateInstanceRef = foreign
		if foreign.R == nil {
			foreign.R = &dateInstanceR{}
		}
		foreign.R.DateInstanceRefVenueSuggestions = append(foreign.R.DateInstanceRefVenueSuggestions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DateInstanceRefID == foreign.ID {
				local.R.DateInstanceRef = foreign
				if foreign.R == nil {
					foreign.R = &dateInstanceR{}
				}
				foreign.R.DateInstanceRefVenueSuggestions = append(foreign.R.DateInstanceRefVenueSuggestions, local)
				break
			}
		}
	}

	return nil
}

// LoadResolvedVenueRef allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (venueSuggestionL) LoadResolvedVenueRef(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVenueSuggestion interface{}, mods queries.Applicator) error {
	var slice []*VenueSuggestion
	var object *VenueSuggestion

	if singular {
		var ok bool
		object, ok = maybeVenueSuggestion.(*VenueSuggestion)
		if !ok {
			object = new(VenueSuggestion)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVenueSuggestion))
			}
		}
	} else {
		s, ok := maybeVenueSuggestion.(*[]*VenueSuggestion)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVenueSuggestion))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &venueSuggestionR{}
		}
		if !queries.IsNil(object.ResolvedVenueRefID) {
			args[object.ResolvedVenueRefID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &venueSuggestionR{}
			}

			if !queries.IsNil(obj.ResolvedVenueRefID) {
				args[obj.ResolvedVenueRefID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`venue`),
		qm.WhereIn(`venue.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Venue")
	}

	var resultSlice []*Venue
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Venue")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for venue")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for venue")
	}

	if len(venueAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ResolvedVenueRef = foreign
		if foreign.R == nil {
			foreign.R = &venueR{}
		}
		foreign.R.ResolvedVenueRefVenueSuggestions = append(foreign.R.ResolvedVenueRefVenueSuggestions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ResolvedVenueRefID, foreign.ID) {
				local.R.ResolvedVenueRef = foreign
				if foreign.R == nil {
					foreign.R = &venueR{}
				}
				foreign.R.ResolvedVenueRefVenueSuggestions = append(foreign.R.ResolvedVenueRefVenueSuggestions, local)
				break
			}
		}
	}

	return nil
}

// LoadSuggestedByRef allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (venueSuggestionL) LoadSuggestedByRef(ctx context.Context, e boil.ContextExecutor, singular bool, maybeVenueSuggestion interface{}, mods queries.Applicator) error {
	var slice []*VenueSuggestion
	var object *VenueSuggestion

	if singular {
		var ok bool
		object, ok = maybeVenueSuggestion.(*VenueSuggestion)
		if !ok {
			object = new(VenueSuggestion)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeVenueSuggestion))
			}
		}
	} else {
		s, ok := maybeVenueSuggestion.(*[]*VenueSuggestion)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeVenueSuggestion)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeVenueSuggestion))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &venueSuggestionR{}
		}
		args[object.SuggestedByRefID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &venueSuggestionR{}
			}

			args[obj.SuggestedByRefID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SuggestedByRef = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.SuggestedByRefVenueSuggestions = append(foreign.R.SuggestedByRefVenueSuggestions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SuggestedByRefID == foreign.ID {
				local.R.SuggestedByRef = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.SuggestedByRefVenueSuggestions = append(foreign.R.SuggestedByRefVenueSuggestions, local)
				break
			}
		}
	}

	return nil
}

// SetDateInstanceRef of the venueSuggestion to the related item.
// Sets o.R.DateInstanceRef to related.
// Adds o to related.R.DateInstanceRefVenueSuggestions.
func (o *VenueSuggestion) SetDateInstanceRef(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DateInstance) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"venue_suggestion\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"date_instance_ref_id"}),
		strmangle.WhereClause("\"", "\"", 2, venueSuggestionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DateInstanceRefID = related.ID
	if o.R == nil {
		o.R = &venueSuggestionR{
			DateInstanceRef: related,
		}
	} else {
		o.R.DateInstanceRef = related
	}

	if related.R == nil {
		related.R = &dateInstanceR{
			DateInstanceRefVenueSuggestions: VenueSuggestionSlice{o},
		}
	} else {
		related.R.DateInstanceRefVenueSuggestions = append(related.R.DateInstanceRefVenueSuggestions, o)
	}

	return nil
}

// SetResolvedVenueRef of the venueSuggestion to the related item.
// Sets o.R.ResolvedVenueRef to related.
// Adds o to related.R.ResolvedVenueRefVenueSuggestions.
func (o *VenueSuggestion) SetResolvedVenueRef(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Venue) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"venue_suggestion\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"resolved_venue_ref_id"}),
		strmangle.WhereClause("\"", "\"", 2, venueSuggestionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ResolvedVenueRefID, related.ID)
	if o.R == nil {
		o.R = &venueSuggestionR{
			ResolvedVenueRef: related,
		}
	} else {
		o.R.ResolvedVenueRef = related
	}

	if related.R == nil {
		related.R = &venueR{
			ResolvedVenueRefVenueSuggestions: VenueSuggestionSlice{o},
		}
	} else {
		related.R.ResolvedVenueRefVenueSuggestions = append(related.R.ResolvedVenueRefVenueSuggestions, o)
	}

	return nil
}

// RemoveResolvedVenueRef relationship.
// Sets o.R.ResolvedVenueRef to nil.
// Removes o from all passed in related items' relationships struct.
func (o *VenueSuggestion) RemoveResolvedVenueRef(ctx context.Context, exec boil.ContextExecutor, related *Venue) error {
	var err error

	queries.SetScanner(&o.ResolvedVenueRefID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("resolved_venue_ref_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ResolvedVenueRef = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ResolvedVenueRefVenueSuggestions {
		if queries.Equal(o.ResolvedVenueRefID, ri.ResolvedVenueRefID) {
			continue
		}

		ln := len(related.R.ResolvedVenueRefVenueSuggestions)
		if ln > 1 && i < ln-1 {
			related.R.ResolvedVenueRefVenueSuggestions[i] = related.R.ResolvedVenueRefVenueSuggestions[ln-1]
		}
		related.R.ResolvedVenueRefVenueSuggestions = related.R.ResolvedVenueRefVenueSuggestions[:ln-1]
		break
	}
	return nil
}

// SetSuggestedByRef of the venueSuggestion to the related item.
// Sets o.R.SuggestedByRef to related.
// Adds o to related.R.SuggestedByRefVenueSuggestions.
func (o *VenueSuggestion) SetSuggestedByRef(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"venue_suggestion\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"suggested_by_ref_id"}),
		strmangle.WhereClause("\"", "\"", 2, venueSuggestionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SuggestedByRefID = related.ID
	if o.R == nil {
		o.R = &venueSuggestionR{
			SuggestedByRef: related,
		}
	} else {
		o.R.SuggestedByRef = related
	}

	if related.R == nil {
		related.R = &userR{
			SuggestedByRefVenueSuggestions: VenueSuggestionSlice{o},
		}
	} else {
		related.R.SuggestedByRefVenueSuggestions = append(related.R.SuggestedByRefVenueSuggestions, o)
	}

	return nil
}

// VenueSuggestions retrieves all the records using an executor.
func VenueSuggestions(mods ...qm.QueryMod) venueSuggestionQuery {
	mods = append(mods, qm.From("\"venue_suggestion\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"venue_suggestion\".*"})
	}

	return venueSuggestionQuery{q}
}

// FindVenueSuggestion retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindVenueSuggestion(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*VenueSuggestion, error) {
	venueSuggestionObj := &VenueSuggestion{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"venue_suggestion\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, venueSuggestionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "pgmodel: unable to select from venue_suggestion")
	}

	if err = venueSuggestionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return venueSuggestionObj, err
	}

	return venueSuggestionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *VenueSuggestion) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("pgmodel: no venue_suggestion provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(venueSuggestionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	venueSuggestionInsertCacheMut.RLock()
	cache, cached := venueSuggestionInsertCache[key]
	venueSuggestionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			venueSuggestionAllColumns,
			venueSuggestionColumnsWithDefault,
			venueSuggestionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"venue_suggestion\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"venue_suggestion\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to insert into venue_suggestion")
	}

	if !cached {
		venueSuggestionInsertCacheMut.Lock()
		venueSuggestionInsertCache[key] = cache
		venueSuggestionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the VenueSuggestion.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *VenueSuggestion) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	venueSuggestionUpdateCacheMut.RLock()
	cache, cached := venueSuggestionUpdateCache[key]
	venueSuggestionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			venueSuggestionAllColumns,
			venueSuggestionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("pgmodel: unable to update venue_suggestion, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"venue_suggestion\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, venueSuggestionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, append(wl, venueSuggestionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update venue_suggestion row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by update for venue_suggestion")
	}

	if !cached {
		venueSuggestionUpdateCacheMut.Lock()
		venueSuggestionUpdateCache[key] = cache
		venueSuggestionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q venueSuggestionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update all for venue_suggestion")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to retrieve rows affected for venue_suggestion")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o VenueSuggestionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("pgmodel: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), venueSuggestionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"venue_suggestion\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, venueSuggestionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to update all in venueSuggestion slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to retrieve rows affected all in update all venueSuggestion")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *VenueSuggestion) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("pgmodel: no venue_suggestion provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(venueSuggestionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	venueSuggestionUpsertCacheMut.RLock()
	cache, cached := venueSuggestionUpsertCache[key]
	venueSuggestionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			venueSuggestionAllColumns,
			venueSuggestionColumnsWithDefault,
			venueSuggestionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			venueSuggestionAllColumns,
			venueSuggestionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("pgmodel: unable to upsert venue_suggestion, could not build update column list")
		}

		ret := strmangle.SetComplement(venueSuggestionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(venueSuggestionPrimaryKeyColumns) == 0 {
				return errors.New("pgmodel: unable to upsert venue_suggestion, could not build conflict column list")
			}

			conflict = make([]string, len(venueSuggestionPrimaryKeyColumns))
			copy(conflict, venueSuggestionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"venue_suggestion\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(venueSuggestionType, venueSuggestionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to upsert venue_suggestion")
	}

	if !cached {
		venueSuggestionUpsertCacheMut.Lock()
		venueSuggestionUpsertCache[key] = cache
		venueSuggestionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single VenueSuggestion record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *VenueSuggestion) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("pgmodel: no VenueSuggestion provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), venueSuggestionPrimaryKeyMapping)
	sql := "DELETE FROM \"venue_suggestion\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete from venue_suggestion")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by delete for venue_suggestion")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q venueSuggestionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("pgmodel: no venueSuggestionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete all from venue_suggestion")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by deleteall for venue_suggestion")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o VenueSuggestionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(venueSuggestionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), venueSuggestionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"venue_suggestion\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, venueSuggestionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: unable to delete all from venueSuggestion slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "pgmodel: failed to get rows affected by deleteall for venue_suggestion")
	}

	if len(venueSuggestionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *VenueSuggestion) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindVenueSuggestion(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *VenueSuggestionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := VenueSuggestionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), venueSuggestionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"venue_suggestion\".* FROM \"venue_suggestion\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, venueSuggestionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "pgmodel: unable to reload all in VenueSuggestionSlice")
	}

	*o = slice

	return nil
}

// VenueSuggestionExists checks if the VenueSuggestion row exists.
func VenueSuggestionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"venue_suggestion\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "pgmodel: unable to check if venue_suggestion exists")
	}

	return exists, nil
}

// Exists checks if the VenueSuggestion row exists.
func (o *VenueSuggestion) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return VenueSuggestionExists(ctx, exec, o.ID)
}
